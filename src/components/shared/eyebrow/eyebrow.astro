---
import { cn } from '@/lib/utils'

interface Props {
  text: string
  class?: string
}

const { text, class: className } = Astro.props
---

<p class={cn('text-accent text-[22px] font-medium italic', className)}>{`// ${text}`}</p>
